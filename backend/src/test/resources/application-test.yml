spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    locations: classpath:db/migration
  mail:
    host: localhost
    port: 1025
  ai:
    anthropic:
      api-key: sk-ant-api03-test

ai:
  mock: true

management:
  health:
    mail:
      enabled: false

app:
  jwt:
    secret: test-secret-key-that-is-at-least-32bytes!!
    access-token-expiration: 900000
    refresh-token-expiration: 604800000
  frontend:
    url: http://localhost:3000
