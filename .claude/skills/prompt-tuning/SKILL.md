---
name: prompt-tuning
description: skill・コマンドのプロンプトを反復改善する時に使用する。/implement・/frontend・test-patterns・db-migration など既存プロンプトの品質を客観評価・改善したい時に発動する
---

# Empirical Prompt Tuning

プロンプトの品質は書いた本人には分からない。書き手が「明瞭だ」と思うものほど、別エージェントが読むと詰まる。バイアスを排した実行者に実際に動かしてもらい、両面で評価して反復するのが本 skill の核。改善が頭打ちになるまで止めない。

---

## このプロジェクトでのチューニング対象

| 対象 | パス | 優先度 |
|---|---|---|
| `/implement` | `.claude/commands/implement.md` | 高（毎 Step 使用） |
| `/frontend` | `.claude/commands/frontend.md` | 高（毎 Step 使用） |
| `/design` | `.claude/commands/design.md` | 高（毎 Step 使用） |
| `/review` | `.claude/commands/review.md` | 高（毎 Step 使用） |
| `test-patterns` | `.claude/skills/test-patterns/SKILL.md` | 高（毎 Step 使用） |
| `db-migration` | `.claude/skills/db-migration/SKILL.md` | 高（DB 変更時） |
| `/refactor` | `.claude/commands/refactor.md` | 中 |
| `/security` | `.claude/commands/security.md` | 中 |
| `/debug` | `.claude/commands/debug.md` | 中 |
| `/performance` | `.claude/commands/performance.md` | 低 |
| `/ops` | `.claude/commands/ops.md` | 低 |
| `/test` | `.claude/commands/test.md` | 低 |

失敗パターン台帳は `.claude/skills/prompt-tuning/ledger.md` で対象プロンプト単位に管理する。

---

## いつ使うか

**自動検知（Claude が提案）:**
同じコマンド・skill で期待外の動作や手動修正が **会話中に 2 回以上** 発生した場合、Claude が会話の最後に tuning を提案する。ユーザーは「OK」と答えるだけで開始できる。

**手動で呼び出す場面:**
- skill / slash command を新規作成・大幅改訂した直後
- 自動提案前でも「このコマンドを改善したい」と気づいたとき
- 新しい Step 開始前に重要コマンドの品質を確認したいとき

**使わない場面:**
- 1 回限りの偶発的な問題（再現性がない場合は様子見）
- 一回限りの使い捨てプロンプト（評価コストが割に合わない）
- 成功率の改善が目的ではなく、書き手の主観的好みを反映したいだけのとき

---

## ワークフロー

### Iteration 0 — description と body の整合チェック（静的、dispatch 不要）

- frontmatter `description` が謳う trigger / 用途を読む
- body がカバーする範囲を読む
- 乖離があれば iter 1 に進む前に description か body を合わせる
- 例: description「navigation / form filling / data extraction」と書いてあるが body は CLI ref のみ、のような乖離を検出
- これを飛ばすと、subagent は description に合わせて body を「再解釈」し、精度が出ているように見えて実質 skill が要件を満たしていない（false positive）

### 1. ベースライン準備

対象プロンプトを確定し、次の 2 つを用意する:

- **評価シナリオ** 2〜3 種（中央値 1 + edge 1〜2）。現実に起こりうるタスクで、対象プロンプトを実際に適用する場面を想定する。
- **要件チェックリスト**（精度算出のため）。シナリオごとに「成果物が満たすべき要件」を 3〜7 項目で列挙する。精度 % = 満たした項目数 / 全項目数。事前に固定する（後から動かさない）。

**このプロジェクトでの典型シナリオ例:**

| 対象 | 中央値シナリオ | edge シナリオ |
|---|---|---|
| `/implement` | 新ドメイン API の CRUD 実装 | 既存 Entity に関係する副作用を持つ API |
| `/frontend` | 新画面コンポーネントの実装 | 既存 Zustand store を変更する場合 |
| `test-patterns` | Controller テストの追加 | 帳簿アクセス制御（他ユーザー 403）テスト |
| `db-migration` | 新テーブル作成 | 既存テーブルへの NOT NULL カラム追加 |

### 2. バイアス排除実行

指示を「白紙」の実行者に読ませる。Task tool で **新規 subagent を dispatch** する。自己再読で済ませない（直前に書いた文章を客観視することは構造的に不可能）。

並列で複数シナリオを同時実行する場合は単一メッセージ内で複数 Agent 呼び出しを並べる。

### 3. 実行

後述の **subagent 起動契約** に従ったプロンプトを subagent に渡し、シナリオを実行させる。

### 4. 両面評価

戻ってきた結果から次を記録する:

**実行者の自己申告**（subagent のレポート本文から抽出）:
- 不明瞭点 / 裁量補完 / テンプレ適用で詰まった箇所

**Trace 解釈**:
- 各不明瞭点に発生フェーズタグを付ける（Understanding / Planning / Execution / Formatting）
- Understanding フェーズの 1 つの曖昧さが Execution フェーズの連鎖的な失敗として表面化することが多い

**構造化された振り返り**: 各不明瞭点は `Issue / Cause / General Fix Rule` の 3 点で返させる。`General Fix Rule` は「失敗パターン台帳」に供給されるクラスレベルの抽象化。

**指示側の計測**（判定規則は本節で一元定義）:
- **成功/失敗**: `[critical]` タグの付いた要件が全て ○ のときのみ成功（○）。1 つでも × または部分的なら失敗（×）
- **精度**: 要件チェックリストの達成率 %（○=満点, ×=0, 部分的=0.5 で合算 / 全項目数）
- **ステップ数**: Agent tool の usage メタの `tool_uses`（Read / Grep も含める）
- **所要時間**: usage メタの `duration_ms`
- **再試行回数**: subagent の自己申告レポートから抽出
- 失敗時は「どの [critical] 項目が落ちたか」を不明瞭点節に 1 行添える

要件チェックリストには `[critical]` タグ付き項目を **最低 1 つ** 含めること。

### 5. 差分適用

不明瞭点を潰す最小修正をプロンプトに入れる。1 イテレーション 1 テーマ。

- 修正前に「この修正が要件チェックリストのどの項目を満たすか」を明示する
- まず `.claude/skills/prompt-tuning/ledger.md` を参照する。`General Fix Rule` が既存エントリと一致するなら「既存の修正はなぜ再発を防げなかったか」を先に問う

### 6. 再評価

新しい subagent で再度 2→5 を回す（同一 agent は再利用しない: 前回の改善を学習している）。

### 7. 収束判定

**停止基準（連続 2 回で全て満たす）:**
- 新規不明瞭点: 0 件
- 精度の前回比改善: +3 ポイント以下
- ステップ数の前回比変動: ±10% 以内
- duration の前回比変動: ±15% 以内
- **過適合チェック**: 収束判定時に未使用の hold-out シナリオ 1 本を追加評価。精度が直近平均から 15 ポイント以上落ちたら過適合 → baseline シナリオ設計に戻る

---

## 評価軸

| 軸 | 取り方 | 意味 |
|---|---|---|
| 成功/失敗 | [critical] 項目が全て ○ か | 最低ライン |
| 精度 | 要件を何 % 満たしたか | 部分成功の程度 |
| ステップ数 | tool_uses | 指示の無駄遣いの指標 |
| 所要時間 | duration_ms | 認知負荷の代替指標 |
| 再試行回数 | 同じ判断をやり直した回数 | 指示の曖昧さのシグナル |
| 不明瞭点（自己申告） | 実行者が箇条書きで列挙 | 質的な改善材料 |
| 裁量補完箇所（自己申告） | 指示で決まっていなかった判断 | 暗黙の仕様の炙り出し |

**重み付け**: 質的（不明瞭点・裁量補完）を主、量的（時間・ステップ数）を補助とする。

### `tool_uses` の質的解釈

シナリオ間で他シナリオ比 **3〜5 倍以上** なら、その skill は decision-tree index 寄りで自己完結性が低い（実行者が references descent を強いられている）。対処: 「最小完成例 inline」や「いつ references を読むかの指針」を SKILL.md 冒頭に追加。

---

## subagent 起動契約

実行者に渡すプロンプト構造:

```
あなたは <対象プロンプト名> を白紙で読む実行者です。

## 対象プロンプト
<対象プロンプトの本文を全文貼る or Read で読ませるパスを指定>

## シナリオ
<シナリオの状況設定 1 段落>

## 要件チェックリスト（成果物が満たすべき項目）
1. [critical] <最低ラインに含む項目>
2. <通常項目>
...
（判定規則は「ワークフロー 4. 両面評価 / 指示側の計測」節に一元定義。[critical] は最低 1 つ必須。）

## タスク
1. 対象プロンプトに従ってシナリオを実行し、成果物を生成する。
2. 終了時に下記レポート構造で返答する。

## レポート構造
- 成果物: <生成物 or 実行結果サマリ>
- 要件達成: 各項目について ○ / × / 部分的（理由付き）
- Trace（折り畳み可 — 全 OK の場合は `Trace: all OK` の 1 行で十分）:
  - Understanding（指示を読み、メンタルモデルを組み立てる）
  - Planning（アプローチ・順序を決める）
  - Execution（実際に作業する）
  - Formatting（成果物を期待される形に整える）
- 不明瞭点（構造化）:
  - Issue: <観測された現象>
  - Cause: <指示レベルで診断した原因>
  - General Fix Rule: <クラスレベルのルール>
- 裁量補完: 指示で決まっておらず自分の判断で埋めた箇所
- 再試行: 同じ判断をやり直した回数とその理由
```

---

## 環境制約

新規 subagent を dispatch できない環境（既に subagent として動作中、Task tool が無効化されている等）では本 skill は **適用しない**。

- 代替案 1: ユーザーに別 Claude Code セッションで実行を依頼する
- 代替案 2: 「empirical evaluation skipped: dispatch unavailable」と明示報告する
- **NG**: 自己再読で代替する（バイアスが入るので評価結果を信じてはいけない）

**構造審査モード**: 記述の整合性・明瞭性だけをチェックしたい場合は「構造審査モード」として切り分ける。subagent 依頼プロンプトに「今回は構造審査モード: 実行ではなくテキスト整合性チェック」と明記する（empirical の代替ではなく補助）。

---

## 反復の打ち切り基準

- **収束（停止）**: 連続 2 回でワークフロー 7 の全条件を満たす（重要プロンプトは 3 連続）
- **発散（設計を疑う）**: 3 回以上イテレーションしても新規不明瞭点が減らない → プロンプトの設計方針を書き直す
- **リソース打ち切り**: 重要度と改善コストが釣り合わなくなったら止める（80 点で出す判断）

---

## 提示フォーマット

```
## Iteration N

### 変更点（前回差分）
- <修正内容 1 行>
- Pattern applied: <台帳上の Pattern 名、または「(new)」>

### 実行結果（シナリオ別）
| シナリオ | 成功/失敗 | 精度 | steps | duration | retries | Weak phase |
|---|---|---|---|---|---|---|
| A | ○ | 90% | 4 | 20s | 0 | — |
| B | × | 60% | 9 | 41s | 2 | Execution |

### 構造化された振り返り（今回新出）
- <シナリオ B>: [critical] 項目 N が × — <落ちた理由 1 行>
  - Issue / Cause / General Fix Rule

### 台帳更新
- Added: <Pattern 名>（シナリオ B 由来）
- Re-seen: <Pattern 名>（元々 iter K）— 再発を防げなかった理由: <理由>

### 次の修正案
- <最小修正 1 行>

（収束判定: 連続 X 回クリア / 停止条件まであと Y 回）
```

---

## Red flags（合理化に注意）

| 出てくる合理化 | 実態 |
|---|---|
| 「自分で読み直せば同じ効果がある」 | 直前に書いた文章を客観視はできない。必ず新規 subagent を dispatch する |
| 「1 シナリオで充分」 | 1 シナリオは過適合する。最低 2、できれば 3 |
| 「不明瞭点ゼロが 1 回出たから終わり」 | 偶然なこともある。連続 2 回で確定判定 |
| 「複数の不明瞭点を一気に潰そう」 | 何が効いたか分からなくなる。1 イテレーション 1 テーマ |
| 「関連する微修正も純粋に 1 件ずつ別 iter に」 | 逆方向の罠。関連する 2〜3 件の微修正は 1 iter にまとめて良い |
| 「メトリクスが良いから質的フィードバックは無視」 | 時間短縮は痩せすぎのサインにもなる。質的を主に |
| 「同じ subagent を使い回そう」 | 前回の改善を学習している。毎回新規に dispatch する |
